{% layout "layout.liquid" %} {% block content %}
<div>
    <form action="/ballets/{{ballets._id}}?_method=PUT" method="post">
        <fieldset>
            <legend>Edit {{ballets.name}}</legend>
            <label>
                NAME:<input 
                type="text" 
                name="name" 
                placeholder="enter ballet name" 
                value="{{ballets.name}}" />
            </label>
            <label>
                COMPOSED:<input 
                type="text" 
                name="color" 
                placeholder="enter ball name" 
                value="{{ballets.composed}}" />
            </label>
            <label>
                READY TO EAT:<input 
                type="checkbox" 
                name="readyToEat" {% if ballets.readyToEat==true %} checked
                {% endif %} />
            </label>
        </fieldset>
        <input type="submit" value="Edit {{ballets.name}}" />
    </form>
</div>
{% endblock %}